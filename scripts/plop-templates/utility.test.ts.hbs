import { describe, it } from "vitest";
import { {{camelCase utilityName}} } from "{{utilityKebab}}"

describe("{{utilityKebab}}", () => {
  it("smoke", async ({ expect }) => {
    await {{camelCase utilityName}}("implement this test");
  })
})